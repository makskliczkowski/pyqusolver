# QES JAX dependencies (GPU support)

# For GPU installation, we use the specific JAX release link
--find-links https://storage.googleapis.com/jax-releases/jax_cuda_releases.html
jax[cuda12_local]>=0.4.23

# ML and optimization libraries (GPU-safe)
flax>=0.6.0
optax>=0.1.0
